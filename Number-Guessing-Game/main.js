var num= (Math.floor(Math.random() * 100) +1);// generates random numbers//
if (num%2==0)
	console.log("number is even.")//diplays if num is even//
else if (num%2==1)
	console.log("number is odd.");//displays if num is odd//
for (count = 1; count<=10; count++) //for the loop to count 10 times//
{
	guess= prompt("Guess num of your choice");//prompts user t input number//
}